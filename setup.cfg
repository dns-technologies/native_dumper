[metadata]
name = native-dumper
version = 0.3.6.dev1
description = Library for read and write Native format between Clickhouse and file.

[options]
packages = find:
package_dir =
    = src
install_requires =
    base_dumper==0.1.0.dev2
    nativelib==0.2.3.dev3

[options.packages.find]
where = src
